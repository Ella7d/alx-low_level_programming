#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Alaways 0 (sucsess)
 *
 */
int main(void)
{
	int a, b;
	{
	for (a = 0; a < 10; a++)
	putchar(a);
	}
	{
	for (b = 0; b < 10; b++)
	putchar(b);
	}
	putchar(\n);
}
