#include "main.h"

/**
 * more_numbers - prints numbers
 *
 *
 */
void more_numbers(void)
{
	char a, b;
	int i;

	while (i < 10)

	{
		for (a = 0; a <= 14; a++)
		{
		a = b;
		if (n > 9)
		{
			_putchar('1');
			b = a % 10;
		}

		_putchar('0' + b);
		}
	_putchar('\n');
	i++;
	}
}
