#include "main.h"
#include <stdio.h>
/**
 * print_array - prints elements of array
 * @a: pointer
 * @n: integer
 * Return: 0
 *
 */
void print_array(int *a, int n)
{
	int i, arr[];
	*a = &arr[];
	int length = sizeof(arr[]);

	for (i = 0; i < length;
	printf ("%p, ", *a[i]);
}
