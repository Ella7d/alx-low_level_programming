#include "main.h"
#include<string.h>

/**
 * _strlen - string lenght
 * @s: string
 * Return: 0
 *
 */
int _strlen(char *s)
{
	int i;
	char c;

	c = strlen(s);
	for (i = 0; i < c; i++)
	{
	
	}
	return (i);
}
