#include "main.h"
/**
 * *_strcat - concatinate strings
 * @dest: char
 * @src: char
 * Return: always 0
 */
char *_strcat(char *dest, char *src)
{
	char s1;
	char s2;

	_strcat (s1, s2);
	return (0);
}
