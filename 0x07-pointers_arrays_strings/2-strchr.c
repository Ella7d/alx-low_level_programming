#include "main.h"
#include <string.h>
/**
 * _strchr - searches for a string
 * @s: string to be scanned
 * @c: char to be searched
 * Return: 
 */
char *_strchr(char *s, char c)
{
	int i = 0;
	char *r;

	while (i != '\0')
		i++;
	r = *(s ,'c');
	{
		if (r != '\0')
			return (c);
	}
	else
	{
		return (0);
		}
}
