#include "main.h"
/**
 * print_sign - prints signs
 * @n: integer
 * Return: 0
 *
 */
int print_sign(int n)
{
	int i;

	if (i > 0)
	return (1);
	{
	else
	if (i == 0)
	return (0);
	}
	{
	else
	return (-1);
	}
}

