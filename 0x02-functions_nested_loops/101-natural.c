#include "main.h"
#include <stdio.h>
/**
 * main - entry pt
 * @i: integer j: intger sum: sum of numbers
 * Return: 0
 *
 */
int main(void)
{
	int i, j, sum;

	for (i = 0; i < 1024; i++ && i = 3 * i)
	{
	for (j = 0; j < 1024; j++ && j = 5 * j)
	}
	{
	sum = i + j;
	printf("%d\n", sum);
	}
	retutn(0);
}

